language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
# command to run tests

install:
  pip install lxml pytest nose pytest-cov

before_script:
  mkdir -p shippable/codecoverage

script:  
  py.test --cov-report html --cov openpyxl --resultlog=shippable/testresults

notifications:
  email:
    recipients:
      - charlie.clark@clark-consulting.eu
